name: Vouch — check PR author

on:
  pull_request_target:
    types: [opened, reopened]

permissions:
  contents: read
  pull-requests: write

jobs:
  check:
    runs-on: ubuntu-latest
    env:
      VOUCH_APP_ID: ${{ secrets.VOUCH_APP_ID }}
    steps:
      - name: Skip when Vouch app is not configured
        if: ${{ env.VOUCH_APP_ID == '' }}
        run: echo "VOUCH_APP_ID is not configured; skipping vouch check."

      - uses: actions/create-github-app-token@v1
        if: ${{ env.VOUCH_APP_ID != '' }}
        id: app-token
        with:
          app-id: ${{ secrets.VOUCH_APP_ID }}
          private-key: ${{ secrets.VOUCH_APP_PRIVATE_KEY }}

      - uses: mitchellh/vouch/action/check-pr@c6d80ead49839655b61b422700b7a3bc9d0804a9 # v1.4.2
        if: ${{ env.VOUCH_APP_ID != '' }}
        with:
          pr-number: ${{ github.event.pull_request.number }}
          require-vouch: "true"
          auto-close: "true"
        env:
          GITHUB_TOKEN: ${{ steps.app-token.outputs.token }}
